const personName = require('./0.file0')
// const file1Data = require('./1.file1')
const {city, greet} = require('./1.file1')// the program is also called in this instance so any executablecode is run along with this
console.log(personName)
console.log(city)
greet()